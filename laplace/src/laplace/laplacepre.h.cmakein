#ifndef LAP_PRE_H
#define LAP_PRE_H

#ifdef _MSC_VER
#include <ciso646>
#endif

namespace laplace {
  const auto LIB_VERSION = "@PROJECT_VERSION@";
  const auto LIB_GIT_SHA = "@PROJECT_REPO_CURRENT_COMMIT@";
  const auto LIB_BUILD_TIMESTAMP = "@PROJECT_BUILD_TIMESTAMP@";

  using real = double;
  constexpr int DIMS = 3;
}

#endif
